# CI retries make nondeterministic failures visible without blocking the run.
# Retried tests are marked flaky in the JUnit report uploaded by the workflow.
[profile.ci]
retries = 2
flaky-result = "pass"
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci.junit]
path = "junit.xml"
