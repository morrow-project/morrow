# Server crate

The `server` crate builds the `morrow-server` binary and contains the broker
runtime: configuration, WAL, partition logs, durable consumers, routing,
authorization, TLS, HTTP administration, middleware, and optional clustering.

Start with the [server architecture](ARCHITECTURE.md), then see the
[operations guide](../../docs/operations.md) for deployment and configuration.
